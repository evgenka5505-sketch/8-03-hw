# Домашнее задание к занятию «Уязвимости и атаки на информационные системы» Беляева Евгения

### Задание 1

Скачайте и установите виртуальную машину Metasploitable: https://sourceforge.net/projects/metasploitable/.

Это типовая ОС для экспериментов в области информационной безопасности, с которой следует начать при анализе уязвимостей.

Просканируйте эту виртуальную машину, используя **nmap**.

Попробуйте найти уязвимости, которым подвержена эта виртуальная машина.

Сами уязвимости можно поискать на сайте https://www.exploit-db.com/.

Для этого нужно в поиске ввести название сетевой службы, обнаруженной на атакуемой машине, и выбрать подходящие по версии уязвимости.

Ответьте на следующие вопросы:

- Какие сетевые службы в ней разрешены?
- Какие уязвимости были вами обнаружены? (список со ссылками: достаточно трёх уязвимостей)
  
*Приведите ответ в свободной форме.*  

### Решение 1

![1IS](https://github.com/evgenka5505-sketch/8-03-hw/blob/main/img/1IS.png)

![2IS](https://github.com/evgenka5505-sketch/8-03-hw/blob/main/img/2IS.png)

В ходе сканирования виртуальной машины Metasploitable с помощью Nmap было обнаружено 23 открытых TCP-порта. Среди запущенных служб присутствуют FTP, SSH, Telnet, SMTP, DNS, HTTP, Samba, NFS, MySQL, PostgreSQL, VNC, IRC и Apache Tomcat.

Анализ версий сервисов показал наличие большого количества устаревшего программного обеспечения. Также был обнаружен анонимный FTP-доступ и предустановленный root shell на порту 1524.

Наиболее значимые уязвимости:

[Vsftpd 2.3.4 - Backdoor Command Execution](https://www.exploit-db.com/exploits/49757)

[Samba 3.0.20 Username Map Script Command Execution](https://www.exploit-db.com/exploits/16320)

[UnrealIRCd Backdoor Command Execution](https://www.exploit-db.com/exploits/16922)

Полученные результаты подтверждают, что Metasploitable является специально уязвимой системой, предназначенной для обучения методам поиска и эксплуатации уязвимостей.

### Задание 2

Проведите сканирование Metasploitable в режимах SYN, FIN, Xmas, UDP.

Запишите сеансы сканирования в Wireshark.

Ответьте на следующие вопросы:

- Чем отличаются эти режимы сканирования с точки зрения сетевого трафика?
- Как отвечает сервер?

*Приведите ответ в свободной форме.*

### Решение 2

**SYN** - на порты отправляется SYN-пакет, как бы намереваясь открыть настоящее соединение, и ожидает ответ. Наличие флагов SYN|ACK в ответе указывает на то, что порт удаленной машины открыт. Если в ответ приходит RST пакет, то порт закрыт.

```
nmap -sS 192.168.48.130
```
Смотрим в Wireshark по фильтру

```
tcp.flags.syn == 1
```

![syn](https://github.com/evgenka5505-sketch/8-03-hw/blob/main/img/syn.png)

**FIN** - на порты отправляется FIN-пакет ( в TCP заголовок ставится флаг FIN). Если в ответ приходит RST пакет, то порт закрыт. Отсутствие ответа означает, что порт открыт.

```
nmap -sF 192.168.48.130
```

в Wireshark по фильтру

```
tcp.flags.fin == 1
```

![3.png](https://github.com/evgenka5505-sketch/8-03-hw/blob/main/img/fin.png)

**Xmas** - TCP-пакеты отсылаются с установленными флагами PSH, URG и FIN.  Если в ответ приходит RST пакет, то порт закрыт. Отсутствие ответа означает, что порт открыт.

```
nmap -sX 192.168.48.130
```

![4.png](https://github.com/evgenka5505-sketch/8-03-hw/blob/main/img/xmas.png)

**UDP** - на порты  отправляется UDP-пакет без данных для определения доступности.Если ответ ICMP-сообщение "port unreacable", то порт закрыт. Отсутствие ответа означает, что порт открыт.

```
nmap -sU 192.168.48.130
```

Wireshark фильтр

```
udp || icmp
```

![5.png](https://github.com/evgenka5505-sketch/8-03-hw/blob/main/img/udp.png)
